# You should run `minikube tunnel` in another shell session in your computer
# then enter this service external IP with the port (both of these are automatically generated by minikube)
# and enter in your browser.
# TODO: it's not working!!
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-app
  labels:
    app: {{ .Release.Name }}
    service: app
spec:
  type: LoadBalancer
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: {{ .Release.Name }}
    service: app